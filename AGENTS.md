# Project Development Directives

## 發布與更新保護原則 (Publishing & Update Directives)

- **核心規則**：每次進行程式碼修改、修復 Bug、發布或重新建置（Build / Deploy / Publish）時，**僅能更新應用程式程式碼邏輯與功能，嚴格禁止覆蓋或重置使用者在後台所編輯之登山行程內容**。
- **資料保護範圍**：包括使用者在後台所新增、編輯、修改、封存或刪除之所有行程資料（行程名稱、副標題、起迄日期、天數、費用、路程檢點、地圖、說明、裝備須知、封面圖片等）。
- **唯一真實來源 (Single Source of Truth)**：以 Cloud SQL 資料庫中目前儲存的實際資料為絕對依歸，嚴格禁止在程式中硬編碼預設資料覆寫資料庫。
- **自動保護機制**：
  1. 伺服器啟動與部署時，具備 `data/.db_initialized` 防覆蓋防線，嚴禁重新注入預設或初始資料。
  2. 任何在後台的編輯或刪除操作，皆即時同步至 Cloud SQL 與備份檔。
